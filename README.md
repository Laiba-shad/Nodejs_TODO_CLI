Node Todo CLI
A simple command‑line Todo application built with Node.js..
It demonstrates interactive menus, task management, and clean separation of logic — perfect for learning CLI development or using as a lightweight starter project.

🚀 Features
Add tasks directly from the terminal

View all tasks in a clean list format

Interactive menu powered by Node’s readline module

Lightweight, dependency‑free implementation

📦 Installation
bash
# Clone the repository
git clone https://github.com/Laiba-shad/Nodejs_TODO_CLI.git

# Navigate into the project
cd Nodejs_TODO_CLI

# Install dependencies (if any are added later)
npm install
▶️ Usage
Run the app with:

bash
node index.js
You’ll see a menu:

Code
1: Create Task
2: View Tasks
3: Exit
🛠️ Project Structure
Code
src/
  index.js        # Main CLI entry point
🌱 Future Improvements
Save tasks to a file (JSON or database) for persistence

Add options to delete or mark tasks as complete

Improve menu navigation with keyboard shortcuts

Convert to TypeScript for type safety
